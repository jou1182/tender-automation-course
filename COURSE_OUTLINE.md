# 🎓 منهج الدورة التدريبية
## بناء نظام أتمتة مناقصات ذكي — من الصفر إلى منتج

> **المدرّب:** د. يوسف سليم — قناة JoUTricks
> **المخرج النهائي:** يبني المتدرّب نظاماً كاملاً (سحب آلي + بوت تليجرام + لوحة ويب + ذكاء اصطناعي) لشركته أو عميله.

---

## 🎯 لمن هذه الدورة؟

| الجمهور | ماذا سيكسب |
|---------|------------|
| مبرمج Python مبتدئ–متوسط | مشروع حقيقي متكامل في معرض أعماله |
| مهندس/موظف تقني يريد الأتمتة | يؤتمت عمله اليدوي المتكرر |
| رائد أعمال تقني | منتج قابل للبيع لشركات المقاولات |

**المتطلبات المسبقة:** أساسيات Python (متغيرات، دوال، شرط، حلقة). *لا* يُشترط خبرة في الويب أو قواعد البيانات — نبنيها معاً.

---

## 🗺️ المعمارية التي سنبنيها (الصورة الكبرى)

```
[منصة المناقصات]
      │ سحب كل 5 دقائق
      ▼
[محرك المقارنة الذكي] ── يكتشف الجديد والتعديلات (منع تكرار عربي)
      │
      ▼
[موافقة بشرية] ── عبر تليجرام (اعتماد بضغطة)
      │
      ├──▶ [بوت تليجرام] ── إشعارات + أوامر + ذكاء اصطناعي
      └──▶ [لوحة ويب Flask] ── متابعة + تحليلات + سجل نتائج
```

**الفلسفة التي ستتعلمها:** *"البيانات مصدر حقيقة واحد، والواجهات تخدمه."*

---

## 📚 محاور الدورة (بترتيب البناء)

### 🟢 الوحدة 0 — المقدمة والرؤية (حلقة واحدة · ~15 د)
- **الهدف:** لماذا نبني هذا؟ المشكلة (متابعة يدوية 2–3 ساعات يومياً) والحل (دقيقتان آليتان).
- **المفاهيم:** العائد على الاستثمار، المعمارية الكبرى، جولة في المنتج النهائي.
- **بلا كود** — إشعال الحماس ورسم الخريطة.

### 🟢 الوحدة 1 — تجهيز البيئة وهيكل المشروع (حلقة · ~20 د)
- **الهدف:** بيئة عمل نظيفة قابلة للتكرار.
- **المفاهيم:** `venv`، `requirements.txt`، تنظيم المجلدات، `.env` والأسرار.
- **الملفات:** `requirements.txt` · `.env.example` · `security_vault.py` (تشفير الأسرار).
- **تمرين:** أنشئ بيئتك وثبّت الحزم وشغّل "Hello Tender".

### 🔵 الوحدة 2 — طبقة البيانات: قاعدة البيانات كمصدر حقيقة (حلقتان · ~40 د)
- **الهدف:** لماذا SQLite تكفي لسنوات، وكيف نصمم جداول نظيفة.
- **المفاهيم:** SQLite، وضع WAL، الجداول (منافسات/معلقات/تدقيق)، منع التكرار على مستوى القاعدة.
- **الملفات:** `db_manager.py` (365 سطر — نشرحه دالةً دالة).
- **تمرين:** صمّم جدول `master_tenders` وأضف أول منافسة يدوياً.

### 🔵 الوحدة 3 — محرك السحب المعياري (حلقتان · ~40 د)
- **الهدف:** كيف نجلب البيانات من أي منصة — بمرونة.
- **المفاهيم:** السحب بالكوكيز مقابل Selenium، `selectors.json`، **نمط المحوّل (Adapter Pattern)**.
- **الملفات:** `export_in_progress.py` · `portal_adapter.py` (العقد المعياري — درس معماري ذهبي).
- **تمرين:** اكتب محوّلاً لمنصتك (أو منصة وهمية).

### 🔴 الوحدة 4 — القلب النابض: محرك المقارنة الذكي (3 حلقات · ~60 د) ⭐
- **الهدف:** اكتشاف الجديد والتعديلات دون تكرار كاذب — أصعب وأهم جزء.
- **المفاهيم:** **التطبيع العربي** (توحيد الهمزات والتشكيل)، بناء هوية بديلة (`AUTO-*`)، الطبقات الثلاث لمنع التكرار، `portal_last_seen_date` (فخ NaN الشهير).
- **الملفات:** `engine_core.py` (341 سطر — قلب النظام).
- **تمرين:** أطعِم المحرك بيانات بها همزات مختلفة وأثبت أنه لا يكرّر.

### 🔵 الوحدة 5 — بوت تليجرام التفاعلي (3 حلقات · ~60 د)
- **الهدف:** إشعارات فورية + قرارات بضغطة زر.
- **المفاهيم:** `pyTelegramBotAPI`، الأزرار التفاعلية (callbacks)، المصادقة، الاعتماد/تغيير المهندس، منع الرسائل المكررة.
- **الملفات:** `bot_daemon.py` (نأخذه أقساماً — الأوامر، الأزرار، الجدولة).
- **تمرين:** ابنِ بوتك وأرسل أول إشعار "منافسة جديدة" بزر اعتماد.

### 🔵 الوحدة 6 — لوحة الويب (Flask) (3 حلقات · ~60 د)
- **الهدف:** واجهة متابعة احترافية متجاوبة.
- **المفاهيم:** Flask، `render_template_string`، الجلسات وتسجيل الدخول، SSE (تحديث فوري)، التصميم المتجاوب (جدول ← بطاقات على الجوال).
- **الملفات:** `web_dashboard.py` · `dashboard_templates.py`.
- **تمرين:** اعرض منافساتك في جدول، ثم حوّله لبطاقات على الجوال.

### 🟣 الوحدة 7 — المساعد الذكي (GPT) (حلقتان · ~40 د)
- **الهدف:** مساعد يجيب بالعربية عن بيانات نظامك.
- **المفاهيم:** OpenAI API، حقن السياق الحي، قاعدة المعرفة، الذاكرة الدائمة، حصر النطاق (لا يجيب خارج تخصصه).
- **الملفات:** `ai_assistant.py` · `chat_handler.py`.
- **تمرين:** اسأل مساعدك "كم منافسة تغلق هذا الأسبوع؟" وأجب من قاعدتك.

### 🟢 الوحدة 8 — الجدولة والأتمتة (حلقة · ~20 د)
- **الهدف:** النظام يعمل وحده 24/7.
- **المفاهيم:** مكتبة `schedule`، الوظائف الدورية (فحص 5د، إحاطة صباحية، نسخ احتياطي).
- **الملفات:** قسم الجدولة في `bot_daemon.py`.
- **تمرين:** اجدول فحصاً كل دقيقة وراقب السجل.

### 🟠 الوحدة 9 — الموثوقية والحراسة الذاتية (حلقتان · ~40 د) ⭐
- **الهدف:** نظام يحرس نفسه ويستدعيك فقط عند العجز.
- **المفاهيم:** الحارس الخارجي (الميت لا يعلن وفاته)، رقيب النسخ (الصمت = سلامة)، `/healthz` (النبض العميق)، بوابة الاختبار قبل النشر.
- **الملفات:** `bot_watchdog.sh` · `backup_sentinel.sh` · `tests/run_all.py` · نقطة `/healthz`.
- **تمرين:** أوقف خدمة عمداً وشاهد الحارس ينعشها.

### 🔴 الوحدة 10 — النشر على السيرفر (VPS) (حلقتان · ~40 د)
- **الهدف:** من جهازك إلى سيرفر سحابي يعمل دائماً.
- **المفاهيم:** Ubuntu، `systemd` (خدمات دائمة)، Cloudflare Tunnel (رابط آمن)، سير النشر الآمن.
- **الملفات:** `SETUP.md` (قسم النشر) · ملفات `.service` · سكريبتات النشر.
- **تمرين:** انشر نسختك على VPS وافتحها برابط عام.

### 🟡 الوحدة 11 — من نظام إلى منتج (حلقتان · ~40 د) ⭐
- **الهدف:** حوّل نظامك لقالب يُباع لأي شركة -- من "نسخة معزولة يدويًا" إلى "معالج ذاتي الخدمة".
- **المفاهيم:** فصل الكود عن البيانات، ملف الهوية الواحد، الميلاد النظيف، النسخ المعزولة، **معالج الإعداد الذاتي** (Wizard) كواجهة ويب تولّد حزمة عميل كاملة (هوية + توكن مشفّر بمفتاح خاص بالعميل + قاعدة بيانات مهيأة) من فورم واحد، و**محول الإكسل اليدوي** كحل عملي لأي عميل بلا سكرابر مخصص -- نفس محرك الدمج والبوت واللوحة يعملون بلا أي تغيير.
- **الملفات:** `company_profile.json` · `provision_instance.py` · `new_tenant.py` · `onboarding_wizard.py` (المعالج ذاتي الخدمة، غير مُضمَّن في هذا المستودع التعليمي العام -- انظر مستودع الإنتاج) · `portal_adapter.py` (ManualExcelPortalAdapter).
- **تمرين:** ولّد نسخة معزولة لشركة "تجريبية" بهوية مختلفة كاملة عبر `new_tenant.py`، وراجع كود `ManualExcelPortalAdapter` في `portal_adapter.py` لتفهم كيف يعمل الإدخال اليدوي.

---

## 🎬 صيغة الحلقات المقترحة (لـ JoUTricks)

- **الطول:** 15–25 دقيقة للحلقة (تركيز عالٍ).
- **البنية:** مشكلة (2د) → مفهوم (5د) → كود مباشر (12د) → تلخيص وتمرين (3د).
- **الإجمالي:** ~28 حلقة · ~10 ساعات · **12 وحدة**.
- **الكود المرافق:** مستودع GitHub بفرع لكل وحدة (`module-01`, `module-02`...).

---

## 💡 مبادئ تعليمية نطبّقها

1. **بناء تدريجي (Build-Along):** كل وحدة تضيف طبقة عاملة — لا نظريات معلّقة.
2. **الأنماط لا الحفظ:** نعلّم *لماذا* (نمط المحوّل، مصدر الحقيقة الواحد) لا *ماذا* فقط.
3. **الأخطاء الحقيقية:** نعرض أخطاء وقعت فعلاً (فخ NaN، الإخفاء الافتراضي) وكيف صيدت — أثمن من الكود الصحيح.
4. **الإثبات لا الادعاء:** كل ميزة تُختبر أمام المتدرّب قبل "تمّت".

---

## 📦 مواد جاهزة نبني عليها (موجودة بالفعل)

| المادة | تُستخدم في |
|--------|-----------|
| `V4_SSOT_Architecture.md` | مرجع المعمارية لكل الوحدات |
| `SETUP.md` | الوحدتان 10–11 (النشر والمنتجة) |
| `دليل_أوامر_البوت.md` | الوحدة 5 (البوت) |
| ملفات `knowledge/*.md` | الوحدة 7 (الذكاء) — كمثال قابل للتعميم |
| `tests/run_all.py` | الوحدة 9 (الجودة) |

---

## ✅ الخطوات التالية لإطلاق الدورة

- [ ] **هذا الملف** — منهج الدورة (تمّ ✅)
- [ ] سكربت الحلقة 0 (المقدمة) — النص التفصيلي أمام الكاميرا
- [ ] تجهيز مستودع GitHub تعليمي (نسخة معمّمة، بلا أسرار)
- [ ] تسجيل الحلقة التجريبية (Pilot) لقياس التفاعل
- [ ] صفحة هبوط بسيطة للتسجيل في الدورة

---

*هذا المنهج جزء من تحويل نظام الرواف إلى أصل تعليمي وتجاري — البنية التقنية جاهزة، والمحتوى يُبنى فوقها.*
